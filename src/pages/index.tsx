import React from 'react';
import BotComponent from '../components/BotComponent';

const Home: React.FC = () => {
    return (
        <div>
            <BotComponent />
        </div>
    );
};

export default Home;